[project]
name = "interview-coach"
version = "0.1.0"
description = "Post-interview gap analysis: record locally, transcribe on-device, get a Claude-powered report"
requires-python = ">=3.10"
dependencies = [
    "mlx-whisper>=0.4.0",
    "jsonschema>=4.0",
    "anthropic>=0.60",
]
